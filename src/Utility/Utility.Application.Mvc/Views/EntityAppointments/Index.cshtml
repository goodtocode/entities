@model IEnumerable<GoodToCode.Subjects.Models.EntityAppointment>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EntityAppointmentKey)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ModifiedDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AppointmentKeyNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EntityKeyNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RecordStateKeyNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EntityAppointmentKey)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ModifiedDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AppointmentKeyNavigation.AppointmentDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EntityKeyNavigation.EntityKey)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RecordStateKeyNavigation.RecordStateName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EntityAppointmentId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EntityAppointmentId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EntityAppointmentId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
