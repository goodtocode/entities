@model IEnumerable<GoodToCode.Entities.Web.Models.ItemType>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ItemTypeKey)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ItemTypeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ItemTypeDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ModifiedDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ItemGroupKeyNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ItemTypeKey)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ItemTypeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ItemTypeDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ModifiedDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ItemGroupKeyNavigation.ItemGroupDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ItemTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ItemTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ItemTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
