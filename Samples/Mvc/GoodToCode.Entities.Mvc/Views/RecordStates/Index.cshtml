@model IEnumerable<GoodToCode.Entities.Web.Models.RecordState>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RecordStateKey)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RecordStateName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ModifiedDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RecordStateKey)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RecordStateName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ModifiedDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RecordStateId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RecordStateId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RecordStateId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
